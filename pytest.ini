[pytest]
testpaths = validateit tests
addopts =
    --flakes
    --cov validateit
    --cov-report term-missing
    --benchmark-columns min,max,mean,stddev
    --benchmark-sort mean
    --benchmark-disable
filterwarnings =
    ignore::pytest.RemovedInPytest4Warning
